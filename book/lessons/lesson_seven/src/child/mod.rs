pub fn get_children() -> Vec<(i32, String, f32)> {
    let children: Vec<(i32, String, f32)> = vec![(1, "Ashe".to_string(), 78.8), (2, "Jeleht".to_string(), 87.9), (3, "Beneth".to_string(), 84.6)];
    return children;
}

