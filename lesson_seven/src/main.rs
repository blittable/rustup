mod sub1;
mod sub2;

fn main() {

    // let a = [1, 2, 3];

    // a.iter().map(|&x| x * 2);

    // assert_eq!(vec![2, 4, 6], doubled);

    //hosting::add_to_waitlist();
    
    sub1::drink_coffee();
    sub2::eat_cake();
}
